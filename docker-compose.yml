version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: baitap6-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped

  redis:
    image: redis:alpine
    container_name: baitap6-redis
    ports:
      - "6380:6379"
    restart: unless-stopped

volumes:
  mongodb_data:
